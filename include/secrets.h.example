#pragma once
// -----------------------------------------------------------------------------
// TEMPLATE — copy this file to "secrets.h" (same folder) and fill in real values.
// secrets.h is gitignored and must NEVER be committed or shared.
// Credentials only — feature on/off switches live in config.h.
// -----------------------------------------------------------------------------
#include <Arduino.h>

// 16-byte AES-128 decryption key from smartmeter-web.wienernetze.at (per meter).
const byte KEY[] = {0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
                    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};

// --- WiFi ---
const char* WIFI_SSID  = "your-ssid";
const char* WIFI_PASS  = "your-pass";

// --- OTA (over-the-air firmware update) ---
const char* OTA_HOSTNAME = "smartmeter2shelly";  // mDNS name -> smartmeter2shelly.local
const char* OTA_PASSWORD = "";                    // "" = no password (set one to secure OTA)

// --- MQTT (used when ENABLE_MQTT=1 in config.h) ---
// Tip: if the broker host resolves to a public IP, prefer its LAN IP here so the
// ESP doesn't depend on NAT-hairpin / DNS overrides.
const char* MQTT_SERVER = "mqtt.example.net";
const int   MQTT_PORT   = 1883;
const char* MQTT_USER   = "user";
const char* MQTT_PASS   = "pass";
const char* MQTT_TOPIC  = "homeassistant/sensor/smartmeter/state";  // JSON state topic
